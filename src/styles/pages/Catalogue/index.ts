import styled from 'styled-components';

export const Container = styled.main`
  max-width: 1200px;
  margin: min(15vw, 8.125rem) auto 0;
  padding: 1.25rem min(1.25rem, 1.57vw);

  color: pink;
  font-size: 3rem;
`;
