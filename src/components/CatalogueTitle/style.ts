import styled from 'styled-components';

export const Container = styled.p`
  font-size: min(1.375rem, 1.7vw);
  color: var(--white_EAE);
  text-transform: uppercase;

  svg {
    max-width: min(0.938rem, 1.2vw);
    margin-right: min(0.625rem, 0.8vw);
  }
`;
